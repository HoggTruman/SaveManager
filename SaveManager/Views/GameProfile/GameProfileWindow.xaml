<Window x:Class="SaveManager.Views.GameProfile.GameProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaveManager.Views.GameProfile"
        mc:Ignorable="d"
        Title="Configure Games / Profiles" 
        MinHeight="400" MinWidth="500"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <local:GameSelectionControl Width="Auto"
                                    Height="60"/>

        <Separator Grid.Row="1" Margin="0 1 0 1"/>

        <local:DirectorySelectionControl Width="Auto"
                                         Height="120"
                                         Grid.Row="2"/>

        <Separator Grid.Row="3" Margin="0 1 0 1"/>

        <local:EditProfilesControl Width="Auto"
                                   Height="200"
                                   Grid.Row="4"/>

    </Grid>
</Window>
