<UserControl x:Class="SaveManager.Views.GameProfile.DirectorySelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaveManager.Views.GameProfile"
             mc:Ignorable="d" 
             MinHeight="130" MinWidth="500"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Label Target="{Binding ElementName=SaveLocationTextBox}" 
           HorizontalAlignment="Left"
           FontSize="14"
           Margin="10,2,0,0"
           Padding="0">
            Location of Savefile:
        </Label>
        <TextBox Name="SaveLocationTextBox" 
              Width="Auto"
              Height="25"
              Grid.Row="1"
              Grid.Column="0"
              Margin="10"/>

        <Button Width="Auto"
            Height="25"
            Grid.Row="1"
            Grid.Column="1"
            Margin="10">Browse</Button>

        <Label Target="{Binding ElementName=ProfilesDirectoryTextBox}" 
               HorizontalAlignment="Left"
               Grid.Row="2"
               FontSize="14"
               Margin="10,2,0,0"
               Padding="0">
            Profiles Directory:
        </Label>
        <TextBox Name="ProfilesDirectoryTextBox" 
                 Width="Auto"
                 Height="25"
                 Grid.Row="3"
                 Margin="10"/>

        <Button Width="Auto"
                Height="25"
                Grid.Row="3"
                Grid.Column="1"
                Margin="10">Browse</Button>
    </Grid>
</UserControl>
