<UserControl x:Class="SaveManager.Views.GameProfile.EditProfilesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaveManager.Views.GameProfile"
             mc:Ignorable="d"
             MinWidth="500" MinHeight="200"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Label Target="{Binding ElementName=ProfilesListBox}" 
               HorizontalAlignment="Left"
               FontSize="14"
               Margin="10,2,0,0"
               Padding="0">
            Profiles:
        </Label>
        <ListBox Name="ProfilesListBox" 
                 Width="Auto"
                 Height="Auto"
                 Grid.Row="1"
                 Grid.RowSpan="4"
                 Margin="10"/>

        <Button Width="Auto"
                Height="25"
                Grid.Row="1"
                Grid.Column="1"
                Margin="10">New</Button>

        <Button Width="Auto"
                Height="25"
                Grid.Row="2"
                Grid.Column="1"
                Margin="10">Rename</Button>

        <Button Width="Auto"
                Height="25"
                Grid.Row="3"
                Grid.Column="1"
                Margin="10">Delete</Button>

    </Grid>
</UserControl>
