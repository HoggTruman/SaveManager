<UserControl x:Class="SaveManager.Components.Notifications.ImportedNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaveManager.Components.Notifications"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <SolidColorBrush x:Key="ImportBrush"
                         Color="#09ad00" />
    </UserControl.Resources>

    <Border BorderBrush="{StaticResource ImportBrush}"
            BorderThickness="3">
        <StackPanel Orientation="Horizontal">
            <Image Source="/Assets/ImportSave.png"
                   Width="30"
                   Margin="10 2" />
            <TextBlock Text="Import Successful"
                       FontSize="24"
                       Foreground="{StaticResource ImportBrush}"
                       Margin="10 2" />
        </StackPanel>
    </Border>
</UserControl>
