<UserControl x:Class="SaveManager.Components.Notifications.ReplacedNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaveManager.Components.Notifications"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <SolidColorBrush x:Key="ReplaceBrush"
                         Color="#ff9500" />
    </UserControl.Resources>

    <Border BorderBrush="{StaticResource ReplaceBrush}"
            BorderThickness="3">
        <StackPanel Orientation="Horizontal">
            <Image Source="/Assets/ReplaceSave.png"
                   Width="30"
                   Margin="10 2" />
            <TextBlock Text="Replace Successful"
                       FontSize="24"
                       Foreground="{StaticResource ReplaceBrush}"
                       Margin="10 2" />
        </StackPanel>
    </Border>
</UserControl>
