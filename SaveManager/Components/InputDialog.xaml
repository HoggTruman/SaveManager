<Window x:Class="SaveManager.Components.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaveManager.Components"
        mc:Ignorable="d"
        Title="Input"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ContentRendered="Window_ContentRendered"
        Background="{x:Static SystemColors.ControlBrush}">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Source="/Assets/Input.png"
               Width="50"
               Height="50"
               Margin="0 0 20 0" />

        <StackPanel Grid.Column="1">
            <Label x:Name="PromptLabel"
                   Content="Prompt" />
            <TextBox x:Name="InputTextBox"
                     Text="Input"
                     MinHeight="20"
                     MinWidth="240" />
        </StackPanel>


        <WrapPanel Grid.Row="2"
                   Grid.ColumnSpan="2"
                   HorizontalAlignment="Center"
                   Margin="0,15,0,0">
            <Button x:Name="OkButton"
                    IsDefault="True"
                    Click="OkButton_Click"
                    MinWidth="80"
                    Margin="0,0,30,0">_Ok</Button>

            <Button IsCancel="True"
                    MinWidth="80">_Cancel</Button>
        </WrapPanel>
    </Grid>
</Window>
